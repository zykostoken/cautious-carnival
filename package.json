{
  "name": "clinica-jose-ingenieros",
  "version": "1.0.0",
  "description": "Clinica Psiquiatrica Privada Jose Ingenieros SRL - Web Application",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "migrate": "node scripts/migrate.mjs",
    "migrate:status": "node scripts/migrate.mjs --status",
    "migrate:dry": "node scripts/migrate.mjs --dry-run",
    "setup-db": "node scripts/setup-db.mjs",
    "build": "npm install && node scripts/migrate.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^8.0.0",
    "@netlify/functions": "^2.0.0",
    "bcryptjs": "^3.0.3",
    "nodemailer": "^8.0.2",
    "postgres": "^3.4.8"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.0.0",
    "@types/nodemailer": "^7.0.11",
    "typescript": "^5.9.3"
  }
}
